<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <!--
      This service is the entry point for all incoming FCM messages.
      It will run even when the app is terminated.
    -->
    <service
      android:name=".ReactNativePushkitService"
      android:exported="false">
      <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT" />
      </intent-filter>
    </service>
  </application>
</manifest>
